{% extends 'base.html.twig' %}

{% block title 'Ajout réseaux sociaux' %}

{% block body %}

<div class="container">
    {{form_start(form)}}
        <button type='submit' class="btn btn-primary">Crée</button>
    {{form_end(form)}}
</div>

{% endblock %}